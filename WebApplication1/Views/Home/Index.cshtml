@{
    ViewData["Title"] = "Главная страница";
}

<div class="text-center">
    <h1 class="display-4">Добро пожаловать!</h1>

    @if (User.Identity.IsAuthenticated)
    {
        <p>Вы вошли как: <strong>@User.Identity.Name</strong></p>
        <p>
            Ваша роль:
            @if (User.IsInRole("Admin"))
            {
                <span class="badge bg-success">Администратор</span>
            }
            else
            {
                <span class="badge bg-primary">Пользователь</span>
            }
        </p>

        <div class="mt-4">
            <a asp-controller="Products" asp-action="Index" class="btn btn-primary">Перейти к продуктам</a>
        </div>
    }
    else
    {
        <p>Пожалуйста, войдите в систему для доступа к функциям приложения.</p>
        <div class="mt-4">
            <a asp-controller="Auth" asp-action="Login" class="btn btn-primary me-2">Вход</a>
            <a asp-controller="Auth" asp-action="Register" class="btn btn-secondary">Регистрация</a>
        </div>
    }
</div>